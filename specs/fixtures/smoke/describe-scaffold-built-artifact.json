{
  "fixtureVersion": 1,
  "kind": "cli-smoke-fixture",
  "name": "describe-scaffold-built-artifact",
  "description": "Execute the built hybrid CLI artifact through a deterministic machine-mode describe-scaffold path.",
  "routeNeutral": true,
  "interactive": false,
  "commandLine": {
    "binary": "<native-cli>",
    "subcommand": "describe-scaffold",
    "args": [
      "--json",
      "--request-id",
      "smoke_describe_scaffold_01"
    ]
  },
  "execution": {
    "mode": "built-artifact-cli",
    "timeoutMs": 60000,
    "requiresLogin": false,
    "notes": "This fixture exercises the generated dist artifact directly without requiring Gemini login, browser interaction, or network-dependent media flows."
  },
  "expectations": {
    "stdoutContract": "json-only",
    "stderrContract": "diagnostics-only",
    "allowedTerminalStates": [
      "succeeded"
    ]
  },
  "evidence": {
    "captureSource": "dist/native-cli/hybrid-native-cli-node-core/run-dev.mjs",
    "reason": "Final verification needs at least one real built-artifact smoke path so CI evidence is not limited to fixture registration metadata."
  }
}
